import React from 'react';
import s from "./Profile.module.scss"

const OtherInfo = () => {

    return (
        <div>
            other info
        </div>
    );
};

export default OtherInfo;